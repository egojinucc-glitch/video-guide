name: Build and Deploy Video CMS

on:
  workflow_dispatch:      # 1. 내가 버튼 누르면 실행 (수동)
  schedule:
    - cron: '0 0 * * *'   # 2. 매일 밤 12시(한국 아침 9시)에 자동 실행

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies 📦
        run: |
          pip install gspread oauth2client

      - name: Build Site 🔨
        run: |
          python build.py

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: site
