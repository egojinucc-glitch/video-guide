name: Build and Deploy Video CMS

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies 📦
        run: |
          pip install gspread oauth2client

      - name: Build Site 🔨
        run: |
          python build.py

      # ▼▼▼ 여기가 수정되었습니다 (디버깅용) ▼▼▼
      - name: Check Folder (Debug)
        run: |
          ls -R site

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: site
          token: ${{ secrets.GITHUB_TOKEN }}  # 👈 이 열쇠를 강제로 쥐여줌!
