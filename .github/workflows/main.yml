name: Build and Deploy Video CMS

on:
  push:                # [추가됨] 파일이 업로드(Push) 되면 자동으로 실행!
    branches: ["main"]
  workflow_dispatch:   # 수동 버튼도 유지

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies 📦
        run: |
          pip install gspread oauth2client

      - name: Build Site 🔨
        run: |
          python build.py

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: site
