name: Build and Deploy Video CMS

on:
  workflow_dispatch:  # 수동 실행 버튼 (나중에 자동화 가능)

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies 📦
        run: |
          pip install gspread oauth2client

      - name: Build Site 🔨
        run: |
          python build.py

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: site # build.py가 결과물을 저장하는 폴더
