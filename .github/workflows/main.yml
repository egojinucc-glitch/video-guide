name: Build and Deploy Video CMS

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Python 🐍
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies 📦
        run: |
          pip install gspread

      - name: Build Site 🔨
        env:  # [추가됨] 여기서 금고 열쇠를 파이썬에게 전달합니다
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          python build.py

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: site
          token: ${{ secrets.GITHUB_TOKEN }}
